Install and instantiate the virtual environment

        python3 -m venv env
        source ./env/bin/activate
    
Install the requirements

        pip3 install -r requirements.txt


Initialize a git repository

        git init